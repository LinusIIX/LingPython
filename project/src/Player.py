from engine import Node

class Player(Node):
    def __init__(self):
        super().__init__()
    #def __init__(self, hasProcess, isIntractable, imgName, position=(0,0), rectWidth=(0,0), scriptName=None, animSlots=None, animSubdivs=None, curImgIdx=None):
    #    super().__init__(hasProcess, isIntractable, imgName, position, rectWidth, scriptName, animSlots, animSubdivs, curImgIdx)